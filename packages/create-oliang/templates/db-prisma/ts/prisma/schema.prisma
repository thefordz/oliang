generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
}

// Define your models here, then run `npm run db:push`, e.g.
//
// model User {
//   id        String   @id @default(uuid())
//   name      String
//   email     String
//   createdAt DateTime @default(now())
// }
